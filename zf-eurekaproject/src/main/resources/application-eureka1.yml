eureka:
  instance:
    hostname: eureka1
    prefer-ip-address: true # 使用ip进行注册
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka2:2002/eureka
